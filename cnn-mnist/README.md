# Intro to Machine Learning, Neural Nets, and Convolutional Neural Nets

Using the TensorFlow Keras framework, build a CNN model that will accurately classify the handwritten digits in the MNIST dataset.